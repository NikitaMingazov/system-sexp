# Building
```./build.sh``` (requires clang to be installed)
# Usage
The language doesn't come with much, only the functions in src/intrinsics.h which is designed to be trivially extensible. The examples/ directory shows sample usage and if you were to do anything with this, it would probably be specialised scripting/config as in Lua, after adding desired intrinsics.
