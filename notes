allow setting of eval-symbol
it evals a symbol or a string

sexp_format:
  first arg alignment
  indent by 2 spaces
  lists shorter than X chars stay in one line
  otherwise split on the first arg

make sym a u16 u8 pair into symbolspace?
or remove sym and make it just str

expose list of primitives as a primitive, for self-documentation
expose list of defined symbols

tcc -dM -E - < /dev/null
todo primitive fns:
preprocessor-default, errno
fprintf and primitives

eval fn spot, a macro is something that evals into macro-body
can redefine eval to handle it differently for a lisp-2

fix memory usage and leaks, then rewrite in C++
make failed asserts from stdin sigint (to be caught and sent back to 0 reading)

as root is a progn, (export sym1 sym2) as the last line is equal to
'((mangle-inner sym1)
  (mangle-inner sym2))
and (import module.ss sym1) as filtering the export would work

use cmph for the primitives

add proper errors with line numbers
make hashtable/stack primitives and access linkedlist that way??

sigint to drop thread 0's job and get a new one as most repls
make the interpreter a .so for FFI and wrappers to be easy
and make wrappers agnostic to default primitives to separate concerns

pattern matching on let binds


tailor this for the software side of embedded, with distributed builds, messaging protocols and allocators this would make a legendary portfolio entry for embedded

thread safety; patch symboltable, arenapool, arena
with PDA reader macro generalisation, `T, `c etc. can all be separate macros
